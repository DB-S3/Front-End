import React from 'react';

function Card(props) {
  
    return (
      <div style={{backgroundColor: props.colour, width: props.width, height: props.height, borderRadius: "1vh"}}>
          test
      </div>
    );
  }export default Card;

  